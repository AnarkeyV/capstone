apiVersion: v1
kind: Service
metadata:
  name: capstone-service
  namespace: staging
  labels:
    app: capstone
    environment: staging
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: capstone
    environment: staging
