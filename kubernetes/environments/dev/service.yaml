apiVersion: v1
kind: Service
metadata:
  name: capstone-service
  namespace: dev
  labels:
    app: capstone
    environment: dev
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: capstone
    environment: dev
