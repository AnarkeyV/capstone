apiVersion: v1
kind: Service
metadata:
  name: capstone-canary-service
  namespace: staging
  labels:
    app: capstone-canary-demo
    environment: staging
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: capstone-canary-demo
